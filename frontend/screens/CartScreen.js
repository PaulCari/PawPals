import React from 'react';
import { View, Text } from 'react-native';

const CartScreen = () => (
  <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
    <Text>Carrito de Compras (En construcción)</Text>
  </View>
);
export default CartScreen;